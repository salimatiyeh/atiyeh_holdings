<div class="container mt-4">
  <div class="row justify-content-center">
    <div class="col-12 col-lg-6">
      <div class="form-container p-4 shadow rounded">
        <h1>Create new expense</h1>
        <%= simple_form_for [@house, @expense] do |f| %>
          <div class="form-container">
            <%= f.input :title, label: "Title", required: false %>
            <%= f.input :description, label: "Description", required: false %>
            <%= f.input :category, label: "Category", required: false %>
            <%= f.input :amount, label: "Amount", required: false %>
            <%= f.input :house_id, as: :select, collection: House.all.pluck(:name, :id), prompt: 'Select House', label: "House" %>
            <%= f.input :document, as: :file, required: false %>
          </div>
          <div class="d-flex justify-content-between">
            <div>
              <%= link_to "Cancel", house_expense_path, class: "btn btn-secondary" %>
            </div>
            <div>
              <%= f.submit "Submit", class: "btn btn-primary" %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
